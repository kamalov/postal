fn test(a: [int]) 
begin
    a >> {
        a >> {
            log(it)
        }
    }
end

fn run
begin
    a: [int]
    push(a, 1)
    push(a, 2)
    test(a)
    // a: [int]
    // a >> {
    //     a >> {
    //         log(it)
    //     }
    // }
end
