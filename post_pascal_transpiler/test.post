// fn len<T>(a: [T]) int external
fn push<T>(a: [T], elem: T) external
// fn array_contains<T>(a: [T], value: T) int external
fn array_last<T>(a: [T]) T external

fn test
   ret 1
end

fn run
   a = [str]
   push(a, 'one')
   push(a, 'two')
   b = array_last(a)
   c = test()
   log(b, c)
end
