fn f1(a: int)
    //log(a)
    ret a
end

rec A {
    b: [int]
}

fn get_a()
    a = A {}
    a.b = [int]
    ret a
end

fn run
    //a = 5
    //if not a do log('not a')
    a = get_a()
    push(a.b, 5)
    //b = [int]
    c = -1
    f = 1
    if not -1 - 1 {
        c = 1 + a.b[0]
    }
    log(c)
end
