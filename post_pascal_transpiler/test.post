rec B {
   b: [int]
}
fn run
   a = [int]
   push(a, 1)
   push(a, 2)
   push(a, 3)
   b = B {}
   b.b = a
   for b.b {
      log(it)
      for b.b do log(it)
   }
   for b.b do log(it)
   log('done')
end
